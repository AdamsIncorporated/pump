{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Frontend",
      "type": "shell",
      "command": "start firefox.exe --remote-debugging-port=9222 --new-window; npm start",
      "options": {
        "cwd": "${workspaceFolder}/frontend"
      },
      "isBackground": true,
      "problemMatcher": ["$eslint-stylish"]
    }
  ]
}
